<% unless @page.nil? || @page.tags.empty? %>
<div id="tags">
  <h2><%= t(".title") %></h2>
  <ul class="teasertext">
    <% @page.tags.each do |tag| %>
      <li><%= link_to tag.name, tag_path(tag.name) %></li>
    <% end %>
  </ul>
</div>
<% end %>
