<% if params[:search_term] %>
  <h2><%= t(".results_for", :query => params[:search_term]) %></h2>
<% end %>

<%= 
  render(
    :partial => 'search_result',
    :collection => @results,
    :as => :node
  )
%>
