<% if params[:search_term] %>
  <h1><%= t(".results_for", :query => params[:search_term]) %></h1>
<% end %>

<%= 
  render(
    :partial => 'search_result',
    :collection => @results,
    :as => :node
  )
%>
