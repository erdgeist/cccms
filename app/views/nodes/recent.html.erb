<h1>Recently changed</h1>

<%= will_paginate @nodes %>
<ul id="recent_changes_full_list">
  <%= render partial: "nodes/recent_change_item", collection: @nodes, as: :node %>
</ul>
<%= will_paginate @nodes %>
