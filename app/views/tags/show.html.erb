<h1><%= t(:show_tag_headline) %> »<%=h @tag %>«</h1>

<p class="feed_link">
  <%= link_to t(".subscribe", :tag => @tag),
        rss_tag_url(:tag => @tag, :format => :xml),
        :type => "application/atom+xml" %>
</p>

<%= 
    render(
      :partial => 'custom/partials/no_date_and_author',
      :collection => @pages,
      :as => :page
    )
  %>
  
<%= will_paginate @pages %>
